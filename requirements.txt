# Website Traffic Database - Python Dependencies
# This project includes both data generation scripts and visualization notebooks

# Data Generation Scripts (Python Standard Library Only)
# - random (for generating random data)
# - datetime (for date/time operations)
# No external dependencies required for data generation scripts

# Visualization and Database Connection (External Dependencies)
psycopg2-binary>=2.9.0
pandas>=1.5.0
matplotlib>=3.5.0
seaborn>=0.11.0
jupyter>=1.0.0

# Optional: For enhanced database connectivity
sqlalchemy>=1.4.0

# Development Dependencies (Optional)
# ipykernel>=6.0.0  # For Jupyter notebook support